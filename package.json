{
  "name": "weroi",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,magenta \"npm run start --prefix frontend\" \"npm run backend\"",
    "backend": "cd backend && python -m uvicorn server:app --reload --host 127.0.0.1 --port 8000",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
